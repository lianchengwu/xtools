---
phase: 02-timestamp-window-and-spawn-or-focus
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - crates/xtools-time/src/convert.rs
  - crates/xtools-time/src/app.rs
  - crates/xtools-time/src/main.rs
autonomous: true
requirements:
  - TIME-01
  - TIME-02

must_haves:
  truths:
    - "User can edit Unix seconds or milliseconds and see the other timestamp plus local datetime update immediately"
    - "User can edit local datetime and see seconds and milliseconds follow"
    - "User can click 现在 beside the datetime caption and all three fields become the current instant"
    - "User can one-click copy the seconds digits and the milliseconds digits"
    - "Copy does not close the window; paste still works because the process stays the clipboard owner"
  artifacts:
    - path: crates/xtools-time/src/convert.rs
      provides: Single jiff convert path for now, seconds, milliseconds, and local datetime
      contains: from_now
    - path: crates/xtools-time/src/app.rs
      provides: Live field sync, 现在 refill, two copy controls, inline error
      contains: copy_text
  key_links:
    - from: crates/xtools-time/src/app.rs
      to: crates/xtools-time/src/convert.rs
      via: Every valid edit and 现在 goes through from_seconds / from_millis / from_local / from_now
      pattern: from_seconds
    - from: crates/xtools-time/src/app.rs
      to: egui Context
      via: Copy of current seconds or milliseconds digits uses ctx.copy_text and stays alive
      pattern: copy_text
    - from: crates/xtools-time/src/convert.rs
      to: jiff Timestamp
      via: from_second / from_millisecond / TimeZone::system / strftime LOCAL_FMT
      pattern: Timestamp::from_second
---

## Phase Goal

**As a** Linux desktop user, **I want to** click a function orb to spawn or focus the timestamp window, convert Unix time both ways, and copy results through the shared chrome, **so that** I can convert and copy times from one independent tool window without a second copy.

This plan is Slice 2 of that goal (TIME-01, TIME-02). It lands on the Plan 02-01 window. Spawn-or-focus is already true.

## Artifacts this phase produces

- `crates/xtools-time/src/convert.rs` — jiff-only s / ms / local datetime / now
- Live three-field sync in TimeApp (D-05, D-06, D-07)
- 「现在」 writes the current instant into all three fields (D-11)
- 「复制」 on 秒 and 毫秒 only, via `ctx.copy_text` (D-08, D-12)
- Inline empty/invalid copy from 02-UI-SPEC; last good peers kept

## Prohibitions

- Do not add extra copy targets: no datetime copy, no third format box, no format picker (D-12, D-13).
- Do not add a UTC companion row (D-07).
- Do not add the older datetime crate. One path: jiff Timestamp + TimeZone::system().
- Do not add a second clipboard crate. Copy stays on the egui context.
- Do not close the window after copy (Wayland clipboard owner is this process).

<!-- planner-discipline-allow: chrono -->
<!-- planner-discipline-allow: arboard -->
- Do not overwrite fields on RAISE (D-10 stays with Plan 02-01).
- Do not put chrome hex or a second look in xtools-time. Compose Plan 02-01 widgets.
- Do not run formatters, clippy, or a test suite.

<objective>
On the Plan 02-01 chrome window, make TIME-01 and TIME-02 true: edit any of Unix seconds, Unix milliseconds, or local datetime and the other two update immediately through jiff; 现在 beside 本地时间 writes the current instant into all three; 复制 beside 秒 and 毫秒 copies those digits and flashes 已复制 for 1 s without quitting (D-05, D-06, D-07, D-08, D-11, D-12, D-13). Invalid or empty input keeps last good peers and shows the matching 02-UI-SPEC line under the edited field only.

Purpose: The window becomes the daily convert/copy tool. Phase 3 clones chrome, not this convert path.

Output: cargo run -p xtools-host, clock click, convert both ways, copy seconds and milliseconds, 现在 resets to now.
</objective>

<execution_context>
@/home/perfect/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/perfect/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-timestamp-window-and-spawn-or-focus/02-CONTEXT.md
@.planning/phases/02-timestamp-window-and-spawn-or-focus/02-RESEARCH.md
@.planning/phases/02-timestamp-window-and-spawn-or-focus/02-UI-SPEC.md
@.planning/phases/02-timestamp-window-and-spawn-or-focus/02-01-PLAN.md
@./AGENTS.md
</context>

<interfaces>
Contracts from Plan 02-01 — use them; do not rename.

xtools_ui (egui-chrome): apply_theme, title_strip, labeled_field, copy_button, now_button, inline_error
TimeApp already owns UnixListener + three strings + last-good peers
NativeOptions / RAISE / Focus already shipped

This plan adds crates/xtools-time/src/convert.rs:
- LOCAL_FMT: %Y-%m-%d %H:%M:%S%.3f  (space, no T, no Z, no offset)
- from_now() -> (i64, i64, String)
- from_seconds(s: i64) -> jiff::Result&lt;(i64, i64, String)&gt;
- from_millis(ms: i64) -> jiff::Result&lt;(i64, i64, String)&gt;
- from_local(text: &amp;str) -> jiff::Result&lt;(i64, i64, String)&gt;

Implementation pins (02-RESEARCH Pattern 5): Timestamp::now / from_second / from_millisecond / as_second / as_millisecond / to_zoned(TimeZone::system()) / DateTime::strptime(LOCAL_FMT) / dt.to_zoned(TimeZone::system()) with Compatible disambiguation. If strptime rejects trailing zeros, DateTime FromStr of the same text is the only fallback, still inside convert.rs, not a second UI format (RESEARCH A3).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Live convert seconds, milliseconds, and local datetime</name>
  <files>crates/xtools-time/src/convert.rs, crates/xtools-time/src/app.rs, crates/xtools-time/src/main.rs</files>
  <read_first>
    .planning/phases/02-timestamp-window-and-spawn-or-focus/02-CONTEXT.md (D-05, D-06, D-07, D-11)
    .planning/phases/02-timestamp-window-and-spawn-or-focus/02-RESEARCH.md (Pattern 5, Pitfall 7, Assumption A3)
    .planning/phases/02-timestamp-window-and-spawn-or-focus/02-UI-SPEC.md (Convert, Invalid/empty, Copywriting)
    crates/xtools-time/src/app.rs
    crates/xtools-time/src/main.rs
  </read_first>
  <action>
    After this task a user can type in any one field and watch the other two follow, and can click 现在 beside 本地时间 to refill all three from the clock.

    Add convert.rs with from_now, from_seconds, from_millis, from_local as named in interfaces. Integer fields parse a trimmed decimal i64 with no thousands separators. Local text is LOCAL_FMT only (D-07). ms stays s × 1000 because successful converts rewrite all three strings from the Timestamp (D-06). Do not hand-roll epoch math.

    TimeApp: keep last-good seconds, milliseconds, and local strings. On first map (already D-09) and on now_button clicked, assign from_now() to both the edit buffers and the last-good triple, and clear any inline error (D-11). 现在 sits on the datetime caption row, trailing 本地时间 — not beside the timestamp boxes.

    Each frame, if the user edited 秒 and it parses, write all three fields from from_seconds. Same for 毫秒 via from_millis and 本地时间 via from_local. Update last-good on success. When the edited field is empty or does not parse: leave that field text as typed, do not touch the peer buffers, show only that field's 02-UI-SPEC line via inline_error (empty-state heading plus body, or the matching 秒/毫秒/本地时间 error). Never crash. Only one error line: the field currently being edited.

    Vertical stack stays 秒, 毫秒, 本地时间 (D-05). No extra timezone row. RAISE still must not refill fields.
  </action>
  <acceptance_criteria>
    - convert.rs contains from_now, from_seconds, from_millis, from_local, LOCAL_FMT, Timestamp::from_second
    - Editing a valid seconds value rewrites milliseconds to s*1000 and local datetime to YYYY-MM-DD HH:MM:SS.mmm
    - Editing a valid milliseconds value rewrites seconds and local datetime
    - Editing a valid local datetime rewrites both integer fields
    - 现在 on the 本地时间 caption row writes the current instant into all three fields and clears the error
    - Empty or invalid edit keeps last-good peers and shows the matching 02-UI-SPEC copy under that field only
    - app.rs does not add a second datetime format or a timezone row
    - cargo check -p xtools-time succeeds
  </acceptance_criteria>
  <verify>
    <automated>test -f crates/xtools-time/src/convert.rs &amp;&amp; cargo check -p xtools-time &amp;&amp; grep -n from_now crates/xtools-time/src/convert.rs &amp;&amp; grep -n from_seconds crates/xtools-time/src/convert.rs &amp;&amp; grep -n from_millis crates/xtools-time/src/convert.rs &amp;&amp; grep -n from_local crates/xtools-time/src/convert.rs &amp;&amp; grep -n Timestamp::from_second crates/xtools-time/src/convert.rs &amp;&amp; grep -n now_button crates/xtools-time/src/app.rs &amp;&amp; grep -n LOCAL_FMT crates/xtools-time/src/convert.rs</automated>
  </verify>
  <done>TIME-01 + D-05 + D-06 + D-07 + D-11: live three-field convert and 现在 refill through jiff.</done>
</task>

<task type="auto">
  <name>Task 2: Copy seconds and milliseconds only</name>
  <files>crates/xtools-time/src/app.rs</files>
  <read_first>
    .planning/phases/02-timestamp-window-and-spawn-or-focus/02-CONTEXT.md (D-08, D-12, D-13)
    .planning/phases/02-timestamp-window-and-spawn-or-focus/02-RESEARCH.md (Pattern 6 copy, Pitfalls 5 7)
    .planning/phases/02-timestamp-window-and-spawn-or-focus/02-UI-SPEC.md (Copy TIME-02 narrowed, button states)
    crates/xtools-time/src/app.rs
  </read_first>
  <action>
    After this task a user can copy the seconds digits or the milliseconds digits with one click and paste them into another app while the window stays up.

    Wire the two chrome copy_button controls already placed on the 秒 and 毫秒 rows (D-08). On click, if that field currently parses as i64, ctx.copy_text the exact digits in that field (10-digit seconds or 13-digit milliseconds when valid). Record copied_at from ctx.input time and request_repaint_after 1 s so the button label is 已复制 then 复制 again. No toast.

    Disable the copy control (secondary fill, muted text per 02-UI-SPEC) when that row is empty or invalid. Do not put a copy control on the datetime row.

    Keep the process alive after copy (Pitfall 5). Title-bar close remains the only quit (D-15 from Plan 02-01).

    TIME-02 this phase is those two integer copies only (D-12). Do not add extra copy targets or a custom format box (D-13). ROADMAP success criterion 4 still names more formats; honor CONTEXT, not that sentence.
  </action>
  <acceptance_criteria>
    - app.rs calls copy_text for the seconds field and for the milliseconds field
    - 复制 on 秒 copies the current seconds digits when valid
    - 复制 on 毫秒 copies the current milliseconds digits when valid
    - The clicked button shows 已复制 for about 1 s then 复制 again
    - Copy does not exit the process
    - No copy control on the datetime row
    - Disabled copy when that field is empty or invalid
    - cargo check -p xtools-time succeeds
    - app.rs has no third copy path (no datetime copy, no extra format copy)
  </acceptance_criteria>
  <verify>
    <automated>cargo check -p xtools-time &amp;&amp; grep -n copy_text crates/xtools-time/src/app.rs &amp;&amp; grep -n copy_button crates/xtools-time/src/app.rs &amp;&amp; grep -n 已复制 crates/xtools-ui/src/chrome.rs crates/xtools-time/src/app.rs &amp;&amp; awk '/./' crates/xtools-time/src/*.rs | grep -v '^[[:space:]]*//' | grep -cE 'chrono|arboard' | grep -qx 0</automated>
    <human-check>cargo run -p xtools-host. Open the clock window. Confirm now-filled 秒 / 毫秒 / 本地时间. Change seconds: milliseconds and local datetime follow. Change milliseconds: seconds and datetime follow. Change local datetime (YYYY-MM-DD HH:MM:SS.mmm): both integers follow. Click 现在: all three jump to the current instant. Type garbage in seconds: milliseconds and datetime stay at last good values and the 秒 error line appears. Click 复制 on 秒 and paste into a terminal: the seconds digits appear; the window is still open and the button briefly reads 已复制. Repeat for 毫秒. Confirm there is no copy control on 本地时间 and no extra format field.</human-check>
  </verify>
  <done>TIME-02 narrowed: one-click copy of seconds and milliseconds only; process stays up.</done>
</task>

</tasks>

<threat_model>
ASVS L1. Same personal desktop process as Plan 02-01. New boundary is typed field text.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Keyboard / paste → convert.rs | Untrusted decimal and datetime strings |
| TimeApp → compositor clipboard | copy_text publishes field digits to whatever the user pastes next |

No network. No secrets.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-07 | Denial of service | convert.rs parse | medium | mitigate | Trimmed i64 only; jiff from_second / from_millisecond / strptime return Result; keep last-good peers; do not panic |
| T-02-08 | Tampering | convert.rs | low | mitigate | Single jiff path. No hand-rolled epoch. Compatible disambiguation on DST gaps |
| T-02-09 | Information disclosure | copy_text | low | accept | Clipboard holds the digits the user just asked to copy on a personal box. Do not log field values at info/warn |
| T-02-05 | Information disclosure | app.rs | low | mitigate | Continue Plan 02-01: do not log tokens or pointer data |
| T-02-SC | Tampering | no new crates | low | accept | This plan adds no registry packages. jiff already OK in 02-01 |

Accepted residual: a huge paste into TextEdit is bounded by egui widget text, then fails i64/jiff parse (low, accept).
</threat_model>

<verification>
- cargo check -p xtools-time
- convert.rs exports from_now / from_seconds / from_millis / from_local
- app.rs uses copy_text and now_button
- Live human-check of convert both ways, 现在, copy seconds, copy milliseconds, invalid field
- Do not run formatters, clippy, or a test suite for this plan
</verification>

<success_criteria>
- TIME-01: Unix seconds or milliseconds convert to local datetime and the other way around; peers follow immediately
- TIME-02 (narrowed by D-12): one-click copy of the 10-digit seconds value and the 13-digit milliseconds value only
- D-05, D-06, D-07, D-08, D-11, D-12 held; D-13 remains deferred
- LAUNCH-01 / LAUNCH-02 / UI-02 already delivered by 02-01 and still true
</success_criteria>

<output>
Create `.planning/phases/02-timestamp-window-and-spawn-or-focus/02-02-SUMMARY.md` when done. Do not commit.
</output>
